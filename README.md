# ZeePal's .dotfiles

## Usage
Intended to be installed via [workstation-kubuntu](https://github.com/ZeePal/workstation-kubuntu).
- Install all packages: `./install`
- Check all packages: `./install --check`
- Install specific package: `./install ripgrep`
- Check specific package: `./install --check ripgrep`
